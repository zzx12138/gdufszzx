#include <iostream>
using namespace std;
int main (){
	cout<<"请输入五个字符及若干个空格：";
	unsigned kg = 0;
	char ch[100];//自己定义大小
      cin.get(ch,sizeof(ch));
		for(int i=0;i<sizeof(ch);i++)
		{
		if (ch[i] == ' ') kg++;
		}
	cout << "空格的个数为：" <<kg<< endl;
	
	system("pause");
	return 0;
}
